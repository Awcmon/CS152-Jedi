write("Testing pass-by-text/name ...")

def fun1 = lambda(x, y) "x = " + x
fun1("abc", abc)

def fun2 = lambda(x, y) (x + x) + (y * y)
fun2({write("thawing operand1"); 2 * 6}, {write("thawing operand2"); 1 + 3})

def times2 = lambda(y) {def gamma = 2; gamma * y}
def gamma = 10
times2(gamma + 1)